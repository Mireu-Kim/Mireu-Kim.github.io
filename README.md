# Mireu-Kim.github.io

포트폴리오는 `app/` 아래 Vite + React 프로젝트입니다. 배포 후 **사이트가 README처럼만 보이면** GitHub 저장소 **Settings → Pages → Build and deployment**에서 Source가 **GitHub Actions**인지 확인하세요. **Deploy from a branch**를 쓰는 경우에는 `main` 루트에 있는 `index.html`과 `assets/`(이 저장소에 동기화된 빌드 결과)가 서빙됩니다.